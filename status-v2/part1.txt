('-').map(Number),n=new Date;return Math.round((Date.UTC(a[0],a[1]-1,a[2])-Date.UTC(n.getFullYear(),n.getMonth(),n.getDate()))/864e5)},E=(e,s)=>s==='critical'?e.trainings.some(t=>D(t.dueDate)<=5):s==='upcoming'?e.trainings.some(t=>{let d=D(t.dueDate);return d>5&&d<=30}):s==='valid'?e.trainings.every(t=>D(t.dueDate)>=0):1,T=t=>D(t.dueDate)<0?'expired':D(t.dueDate)<=5?'critical':D(t.dueDate)<=30?'upcoming':'valid',C=s=>s==='upcoming'?'warning':s==='valid'?'success':'danger',N=e=>E(e,'critical')?'critical':E(e,'upcoming')?'upcoming':'valid';
function X(){let a=A();if(!a)return;let r=[];a.employees.forEach(e=>e.trainings.forEach(t=>r.push({e,t,d:D(t.dueDate)})));return{a,r,c:r.filter(x=>x.d<=5),u:r.filter(x=>x.d>5&&x.d<=30),v:r.filter(x=>x.d>=0),ce:a.employees.filter(e=>E(e,'critical')),ue:a.employees.filter(e=>E(e,'upcoming')),ve:a.employees.filter(e=>E(e,'valid'))}}
function I(k){let p=k==='c'?'<path d="M12 3 2 20h20Z"/><path d="M12 9v4M12 17h.01"/>':k==='u'?'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>':'<path d="m5 12 4 4L19 6"/>';return`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">${p}</svg>`}
function K(s,z,i,t,d,a,b,c,e){return`<button class="kpi card ${z} v2k ${S===s?'on':''}" data-v2="${s}"><div class="kpi-top"><div class="kpi-icon">${I(i)}</div><div><b>${t}</b><small>${d}</small></div></div><div class="v2m"><div><strong>${a}</strong><span>${b}</span></div><i></i><div><strong>${c}</strong><span>${e}</span></div></div></button>`}
function Q(){let x=X(),g=document.querySelector('.kpi-grid');if(!x||!g||g.dataset.v2)return;let l=U();g.dataset.v2=1;g.innerHTML=K('critical','danger','c',l[0],l[1],x.c.length,l[6],`${x.ce.length}/${x.a.employees.length}`,l[7])+K('upcoming','warning','u',l[2],l[3],x.u.length,l[6],`${x.ue.length}/${x.a.employees.length}`,l[7])+K('valid','success','v',l[4],l[5],`${x.v.length}/${x.r.length}`,l[8],`${x.ve.length}/${x.a.employees.length}`,l[9]);g.querySelectorAll('[data-v2]').forEach(b=>b.onclick=()=>O(b.dataset.v2));document.querySelectorAll('.progress-row').forEach(r=>{let dep=r.querySelector('.progress-label')?.textContent.trim(),q=x.a.employees.filter(e=>e.department===dep),n=q.filter(e=>E(e,'valid')).length,p=q.length?Math.round(n/q.length*100):0;let b=r.querySelector('.progress-bar'),v=r.querySelector('.progress-value');if(b.style.width!==p+'%')b.style.width=p+'%';if(v.textContent!==p+'%