(()=>{'use strict';
const seed=window.SafeTrackSeed,api=window.__SafeTrack,employeeApi=window.__SafeTrackEmployeeGroups;
if(!seed||!api||!employeeApi)return;
const LANG={de:'Deutsch',pl:'Polski',ru:'Русский',ar:'العربية',tr:'Türkçe',hu:'Magyar',ro:'Română'};
const RANK={critical:0,soon:1,valid:2},LABEL={critical:'Kritisch',soon:'In 6–30 Tagen fällig',valid:'Gültig'};
const E=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const normalize=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
const today=()=>new Date().toISOString().slice(0,10);
const productVersion=()=>api.productVersion||api.version||'SafeTrack v0.24';
let selectedTrainings=new Set(),selectedEmployees=new Set(),sortMode='status',searchText='',groupContext=null;
const toast=text=>{const t=document.querySelector('#toast');if(!t)return;t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)};
const trainingById=id=>api.catalog.find(t=>t.id===id);
const trainingTitle=(t,l='de')=>t?.title?.[l]||t?.title?.de||t?.id||'';
const eligibleForTraining=(employee,t)=>t?.active!==false&&Array.isArray(t?.roles)&&t.roles.some(r=>r==='all'||r===employee[4]);
function relevantAssignments(employee){const byId=new Map(employeeApi.assignments(employee[1]).map(x=>[x.training.id,x]));return[...selectedTrainings].map(id=>byId.get(id)).filter(Boolean)}
function employeeCandidate(employee){const items=relevantAssignments(employee);if(!items.length)return null;const statuses=items.map(x=>x.status),status=statuses.includes('critical')?'critical':statuses.includes('soon')?'soon':'valid';return{employee,items,status,criticalCount:items.filter(x=>x.status==='critical').length,soonCount:items.filter(x=>x.status==='soon').length}}
function candidates(){return seed.employees.map(employeeCandidate).filter(Boolean)}
function filteredCandidates(){let rows=candidates(),q=normalize(searchText);if(q)rows=rows.filter(x=>normalize(`${x.employee[0]} ${x.employee[1]}`).includes(q));const cmp={status:(a,b)=>RANK[a.status]-RANK[b.status]||a.employee[0].localeCompare(b.employee[0],'de'),language:(a,b)=>(LANG[a.employee[5]]||a.employee[5]).localeCompare(LANG[b.employee[5]]||b.employee[5],'de')||a.employee[0].localeCompare(b.employee[0],'de'),name:(a,b)=>a.employee[0].localeCompare(b.employee[0],'de'),personnel:(a,b)=>a.employee[1].localeCompare(b.employee[1],'de')}[sortMode];return rows.sort(cmp)}
const badge=status=>`<span class="st-category-status st-${status}"><span class="st-status-icon"></span>${LABEL[status]}</span>`;
function syncTrainingSelection(){document.querySelectorAll('[data-group-training]').forEach(x=>x.checked=selectedTrainings.has(x.dataset.groupTraining));const count=String(selectedTrainings.size);document.querySelectorAll('[data-group-training-count]').forEach(x=>x.textContent=count);document.querySelectorAll('[data-group-action="assign-employees"]').forEach(x=>x.disabled=!selectedTrainings.size)}
function selectedTrainingSummary(){return[...selectedTrainings].map(trainingById).filter(Boolean).map(t=>`<span class="st-group-training-chip"><span class="st-training-number">${E(t.trainingNumber)}</span>${E(trainingTitle(t))}</span>`).join('')}
function employeeRow(x){const e=x.employee,applicable=x.items.length,details=x.criticalCount?`${x.criticalCount} kritisch${x.soonCount?` · ${x.soonCount} fällig`:''}`:x.soonCount?`${x.soonCount} fällig`:`${applicable} gültig`;return`<label class="st-group-employee-row" data-group-owner="group-training.js" data-v019-pnr="1" data-status="${x.status}" data-language="${E(e[5])}"><input type="checkbox" data-group-employee="${E(e[1])}" ${selectedEmployees.has(e[1])?'checked':''}><span class="st-group-employee-copy"><strong>${E(e[0])}</strong><small><span class="st-pnr-label">PNr.</span> <strong class="st-pnr-value">${E(e[1])}</strong> · ${E(e[2])} · ${E(e[3])}</small><small>${E(LANG[e[5]]||e[5])} · ${applicable} passende Unterweisung(en) · ${E(details)}</small></span>${badge(x.status)}</label>`}
function employeeListMarkup(){const rows=filteredCandidates();if(!rows.length)return'<div class="empty">Keine passenden Mitarbeitenden gefunden.</div>';if(sortMode!=='language')return rows.map(employeeRow).join('');let out='',last='';for(const x of rows){const lang=LANG[x.employee[5]]||x.employee[5];if(lang!==last){last=lang;const count=rows.filter(y=>(LANG[y.employee[5]]||y.employee[5])===lang).length;out+=`<div class="st-language-group-head"><strong>${E(lang)}</strong><span>${count} Mitarbeitende</span></div>`}out+=employeeRow(x)}return out}
function renderEmployeePicker(){const host=document.querySelector('[data-group-employee-list]');if(host)host.innerHTML=employeeListMarkup();document.querySelectorAll('[data-group-selected-count]').forEach(x=>x.textContent=String(selectedEmployees.size));document.querySelectorAll('[data-group-action="preview-confirmations"]').forEach(x=>x.disabled=!selectedEmployees.size)}
function openEmployeePicker(){if(!selectedTrainings.size)return toast('Mindestens eine Unterweisung auswählen');selectedEmployees=new Set();sortMode='status';searchText='';document.querySelector('.st-group-dialog-bg')?.remove();const bg=document.createElement('div');bg.className='st-group-dialog-bg';bg.dataset.groupOwner='group-training.js';bg.innerHTML=`<section class="st-group-dialog" role="dialog" aria-modal="true" aria-label="Mitarbeitende zuweisen"><header class="st-group-dialog-head"><div><h2>Gruppenunterweisung vorbereiten</h2><p>${selectedTrainings.size} Unterweisung(en) ausgewählt</p></div><button class="btn" data-group-action="close-picker">Zurück</button></header><div class="st-group-selected-trainings">${selectedTrainingSummary()}</div><div class="st-group-controls"><label><span>Suchen</span><input type="search" data-group-search placeholder="Name oder PNr."></label><label><span>Sortierung</span><select data-group-sort><option value="status">Status</option><option value="language">Sprache</option><option value="name">Name</option><option value="personnel">PNr.</option></select></label></div><div class="st-group-bulk"><button class="btn" data-group-action="select-critical">Alle Kritischen</button><button class="btn" data-group-action="select-soon">Alle in 6–30 Tagen Fälligen</button><button class="btn" data-group-action="select-all-eligible">Alle passenden</button><button class="btn" data-group-action="clear-employees">Auswahl aufheben</button></div><div class="st-group-employee-list" data-group-employee-list></div><footer class="st-group-dialog-foot"><div><strong><span data-group-selected-count>0</span> Mitarbeitende ausgewählt</strong><small>Nur für die jeweilige Unterweisung passende Personen erscheinen später auf deren Liste.</small></div><div><button class="btn" data-group-action="close-picker">Zurück</button><button class="btn primary" data-group-action="preview-confirmations" disabled>Schulungsbestätigung vorbereiten</button></div></footer></section>`;document.body.appendChild(bg);renderEmployeePicker()}
function selectEmployeesByStatus(status){candidates().filter(x=>x.status===status).forEach(x=>selectedEmployees.add(x.employee[1]));renderEmployeePicker()}
function selectedPeopleForTraining(t){return seed.employees.filter(e=>selectedEmployees.has(e[1])&&eligibleForTraining(e,t)).sort((a,b)=>{if(sortMode==='language'){const d=(LANG[a[5]]||a[5]).localeCompare(LANG[b[5]]||b[5],'de');if(d)return d}return a[0].localeCompare(b[0],'de')})}
function confirmationDocument(t){const people=selectedPeopleForTraining(t);if(!people.length)return'';const date=groupContext?.date||today(),rows=people.map(e=>`<tr><td class="st-group-person-name"><strong>${E(e[0])}</strong></td><td class="st-group-person-number">${E(e[1])}</td><td class="st-group-person-role">${E(e[3])}</td><td class="st-group-signature-cell"></td></tr>`).join('');return`<article class="st-group-confirmation-document" data-group-owner="group-training.js" data-training-id="${E(t.id)}" data-training-number="${E(t.trainingNumber)}" data-employee-count="${people.length}"><header class="st-group-print-header"><div><span class="st-training-number">${E(t.trainingNumber)}</span><h1>Schulungsbestätigung</h1><h2>${E(trainingTitle(t,'de'))}</h2></div><dl><div><dt>Unterweisungs-ID</dt><dd>${E(t.trainingNumber)}</dd></div><div><dt>Version</dt><dd>v${E(t.version)}</dd></div><div><dt>Durchgeführt am</dt><dd>${E(date)}</dd></div><div><dt>Teilnehmende</dt><dd>${people.length}</dd></div></dl></header><table class="st-group-confirmation-table"><colgroup><col class="st-col-person"><col class="st-col-number"><col class="st-col-role"><col class="st-col-signature"></colgroup><thead><tr><th>Mitarbeitende Person</th><th>PNr.</th><th>Tätigkeit</th><th>Unterschrift</th></tr></thead><tbody>${rows}</tbody></table><section class="st-group-instructor-fields"><div><span>Name der unterweisenden / beaufsichtigenden Person</span><strong>${E(groupContext?.instructor||'')}</strong></div><div><span>PNr.</span><strong>${E(groupContext?.instructorPersonnel||'')}</strong></div><div class="st-group-instructor-signature"><span>Unterschrift unterweisende / beaufsichtigende Person</span><strong></strong></div></section><footer class="st-print-footer"><span>${E(productVersion())}</span><span>${E(t.trainingNumber)} · ${people.length} Teilnehmende</span></footer></article>`}
function confirmationDocuments(){return[...selectedTrainings].map(trainingById).filter(Boolean).map(confirmationDocument).filter(Boolean).join('')}
function openConfirmationPreview(){if(!selectedEmployees.size)return toast('Mindestens eine Person auswählen');groupContext={instructor:document.querySelector('[data-group-instructor]')?.value||'',instructorPersonnel:document.querySelector('[data-group-instructor-personnel]')?.value||'',date:document.querySelector('[data-group-date]')?.value||today()};document.querySelector('.st-group-confirmation-preview-bg')?.remove();const bg=document.createElement('div');bg.className='st-group-confirmation-preview-bg';bg.dataset.groupOwner='group-training.js';bg.innerHTML=`<div class="st-group-preview-toolbar"><div><strong>Schulungsbestätigung – Druckvorschau</strong><span>${selectedTrainings.size} Unterweisung(en) · ${selectedEmployees.size} ausgewählte Mitarbeitende</span></div><div><button class="btn" data-group-action="close-confirmation-preview">Zurück</button><button class="btn primary" data-group-action="execute-group-print">Drucken</button></div></div><section class="st-group-preview-documents">${confirmationDocuments()}</section>`;document.body.appendChild(bg);window.__SafeTrackPrint?.prepareGroupPreview?.(bg)}
function openConfirmationSetup(){const picker=document.querySelector('.st-group-dialog');if(!picker)return;picker.querySelector('.st-group-confirmation-setup')?.remove();const panel=document.createElement('section');panel.className='st-group-confirmation-setup';panel.dataset.groupOwner='group-training.js';panel.innerHTML=`<div><h3>Schulungsbestätigung</h3><p>Für jede ausgewählte Unterweisung wird eine eigene Teilnehmerliste erstellt.</p></div><label><span>Durchgeführt am</span><input type="date" data-group-date value="${today()}"></label><label><span>Name der unterweisenden / beaufsichtigenden Person</span><input data-group-instructor placeholder="Name"></label><label><span>PNr. der unterweisenden / beaufsichtigenden Person</span><input data-group-instructor-personnel placeholder="PNr-####"></label><div class="st-group-confirmation-setup-actions"><button class="btn" data-group-action="cancel-confirmation-setup">Zurück</button><button class="btn primary" data-group-action="open-confirmation-preview">Druckvorschau</button></div>`;picker.appendChild(panel)}
function closePicker(){document.querySelector('.st-group-dialog-bg')?.remove();selectedEmployees=new Set();searchText=''}
function closeConfirmationPreview(){document.querySelector('.st-group-confirmation-preview-bg')?.remove()}
document.addEventListener('change',e=>{const training=e.target.closest?.('[data-group-training]');if(training){training.checked?selectedTrainings.add(training.dataset.groupTraining):selectedTrainings.delete(training.dataset.groupTraining);syncTrainingSelection();return}const employee=e.target.closest?.('[data-group-employee]');if(employee){employee.checked?selectedEmployees.add(employee.dataset.groupEmployee):selectedEmployees.delete(employee.dataset.groupEmployee);renderEmployeePicker();return}if(e.target.matches?.('[data-group-sort]')){sortMode=e.target.value;renderEmployeePicker()}},true);
document.addEventListener('input',e=>{if(e.target.matches?.('[data-group-search]')){searchText=e.target.value;renderEmployeePicker()}},true);
document.addEventListener('click',e=>{const b=e.target.closest?.('[data-group-action]');if(!b)return;const a=b.dataset.groupAction;if(a==='execute-group-print')return;if(a==='clear-trainings'){selectedTrainings.clear();syncTrainingSelection()}else if(a==='assign-employees')openEmployeePicker();else if(a==='close-picker')closePicker();else if(a==='select-critical')selectEmployeesByStatus('critical');else if(a==='select-soon')selectEmployeesByStatus('soon');else if(a==='select-all-eligible'){candidates().forEach(x=>selectedEmployees.add(x.employee[1]));renderEmployeePicker()}else if(a==='clear-employees'){selectedEmployees.clear();renderEmployeePicker()}else if(a==='preview-confirmations')openConfirmationSetup();else if(a==='cancel-confirmation-setup')b.closest('.st-group-confirmation-setup')?.remove();else if(a==='open-confirmation-preview')openConfirmationPreview();else if(a==='close-confirmation-preview')closeConfirmationPreview()},true);
document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;if(document.querySelector('.st-group-confirmation-preview-bg'))closeConfirmationPreview();else if(document.querySelector('.st-group-confirmation-setup'))document.querySelector('.st-group-confirmation-setup')?.remove();else if(document.querySelector('.st-group-dialog-bg'))closePicker()});
window.__SafeTrackGroupTraining={owner:'group-training.js',getSelectedTrainings:()=>[...selectedTrainings],getSelectedEmployees:()=>[...selectedEmployees],candidates,selectTrainings(ids){selectedTrainings=new Set((ids||[]).filter(id=>trainingById(id)));syncTrainingSelection()},selectEmployees(ids){selectedEmployees=new Set((ids||[]).filter(id=>seed.employees.some(e=>e[1]===id)));renderEmployeePicker()},setSort(mode){if(['status','language','name','personnel'].includes(mode))sortMode=mode;renderEmployeePicker()},openEmployeePicker,confirmationDocuments};
syncTrainingSelection();
})();