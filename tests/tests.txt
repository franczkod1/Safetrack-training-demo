# Tests

## Test 1 - Mobile hierarchy navigation and valid checkmark [sanity]
Viewport: mobile
Covers: corrected Gültig checkmark, Menu plus Zurück in internal views, employee three-level hierarchy, mobile overflow prevention
Description: Verifies the requested navigation and hierarchy UI on an iPhone-sized viewport.

Steps:
1. Open Übersicht and verify the Gültig card shows a normal checkmark rather than a downward chevron
2. Open Mitarbeitende, expand Produktion and one Tätigkeit; verify Produktion has the darkest neutral surface, Tätigkeit a lighter neutral surface, and employee rows are light/white
3. Open one employee profile and verify both "Menü" and "Zurück" are available at the top; verify there is no horizontal page scrolling

Expected: The checkmark is oriented correctly, hierarchy depth is visually obvious, and mobile internal navigation exposes both Menu and Zurück without overflow.

## Test 2 - Training hierarchy uses the same depth language
Viewport: desktop
Covers: employee training category hierarchy, training catalog hierarchy, status-color separation, desktop regression
Description: Verifies hierarchical training views use neutral darker-to-lighter surfaces without replacing red/yellow/green status meaning.

Steps:
1. Open Mitarbeitende, open one employee, and expand one training category; verify the category header is darker than the individual training rows
2. Return to Unterweisungen and verify category/group headings are visually stronger than the individual training cards
3. Verify critical/soon/valid badges still retain their red/yellow/green status colors and the page remains usable

Expected: Training hierarchy follows the same neutral depth system while status colors retain their original semantic meaning.

## Test 3 - Internal Menu returns to main navigation and central Documents remains reachable
Viewport: mobile
Covers: global internal Menu action, overlay cleanup, sidebar navigation, central v0.23 Documents regression, PNr-#### preservation
Description: Verifies the new internal Menu button actually opens the main navigation and does not regress central document navigation.

Steps:
1. Open an employee profile, choose the new "Menü" button, and verify the main sidebar opens
2. Choose "Dokumente" and verify the central view contains "Dokumente hochladen" and "Erwartete Rückläufe" and does not show "Lokale Prototyp-Nachverfolgung"
3. Return to Mitarbeitende and verify a canonical PNr-#### employee identifier remains visible

Expected: The Menu button exits the internal overlay into the main navigation, central Documents remains active, and employee identifiers are unchanged.