# Tests

## Test 1 - Mobile dashboard shows current German-only status UI [sanity]
Viewport: mobile
Covers: current local dashboard date, removed dashboard start button, removed admin language selector, menu version placement, critical/soon/valid visual states, mobile overflow prevention
Description: Verifies the requested v0.23 dashboard cleanup on an iPhone-sized viewport.

Steps:
1. Open Übersicht and verify the hero shows "HEUTE · 07.08.2026", no "Unterweisung starten" button is present in the hero, and no language selector is visible in the top bar
2. Verify the three KPI cards are visually distinct: Kritisch has a pale red surface with an exclamation symbol, In 6–30 Tagen fällig has a pale yellow surface with a triangle symbol, and Gültig has a pale green surface with a check symbol
3. Open "Menü" and verify "SafeTrack v0.23" is visible at the lower-left of the open sidebar; verify there is no horizontal page scrolling

Expected: The mobile dashboard uses the current date, German-only admin chrome, the requested three status treatments, no hero start button, and the version is visible inside the open menu.

## Test 2 - Dashboard status navigation and Zurück remain functional
Viewport: desktop
Covers: dashboard KPI interaction, filtered employee navigation, Zurück navigation, German admin UI
Description: Verifies the visual bugfix does not break the existing dashboard navigation.

Steps:
1. Open Übersicht and verify the admin interface is German and no language selector is shown
2. Click the "Kritisch" KPI card and verify Mitarbeitende opens with the critical status filter applied
3. Choose "Zurück" and verify Übersicht returns with the three status cards still visible

Expected: Status cards remain interactive, Zurück returns to the dashboard, and removing the language selector does not break navigation.

## Test 3 - Central v0.23 Documents view and employee identifiers are unchanged
Viewport: mobile
Covers: v0.23 Documents navigation regression, expected-return registry, upload control, PNr-#### employee identifiers, no old local registry
Description: Verifies the dashboard-only patch does not regress the central document workflow or employee navigation.

Steps:
1. Open "Menü" and choose "Dokumente"; verify the central v0.23 view contains "Dokumente hochladen" and "Erwartete Rückläufe" and does not show "Lokale Prototyp-Nachverfolgung"
2. Close/return from Dokumente, open Mitarbeitende, expand one area and one job group, and verify a canonical PNr-#### value is visible
3. Open and close one employee profile and verify the mobile page still has no horizontal overflow

Expected: The central v0.23 Documents registry remains active, PNr-#### is preserved, and the dashboard UI patch introduces no mobile regression.