# Tests

## Test 1 - Mobile hierarchy navigation and valid checkmark [sanity]
Viewport: mobile
Covers: corrected Gültig checkmark, Menu plus Zurück in internal views, employee three-level hierarchy, mobile overflow prevention
Description: Verifies the requested navigation and hierarchy UI on an iPhone-sized viewport.

Steps:
1. Open Übersicht and verify the Gültig card shows a normal checkmark rather than a downward chevron
2. Open Mitarbeitende, expand Produktion and one Tätigkeit; verify Produktion has the darkest neutral surface, Tätigkeit a lighter neutral surface, and employee rows are light/white
3. Open one employee profile and verify both "Menü" and "Zurück" are available at the top; verify there is no horizontal page scrolling

Expected: The checkmark is oriented correctly, hierarchy depth is visually obvious, and mobile internal navigation exposes both Menu and Zurück without overflow.

## Test 2 - Training catalog hierarchy remains clear
Viewport: desktop
Covers: training catalog hierarchy, neutral depth surfaces, desktop regression
Description: Quickly verifies the training catalog uses the same darker-heading/light-card hierarchy.

Steps:
1. Open Unterweisungen
2. Verify a category/group heading has a stronger neutral background than the individual white training cards beneath it
3. Verify the catalog remains readable and interactive

Expected: Training categories are visually stronger than individual training cards without changing the SafeTrack status semantics.

## Test 3 - Internal Menu returns to main navigation and central Documents remains reachable
Viewport: mobile
Covers: global internal Menu action, rerender-safe Menu insertion, overlay cleanup, sidebar navigation, central v0.23 Documents regression, PNr-#### preservation
Description: Verifies the new internal Menu button opens the main navigation safely during rerenders and does not regress central document navigation.

Steps:
1. Open an employee profile, choose the new "Menü" button, and verify the main sidebar opens without a page error
2. Choose "Dokumente" and verify the central view contains "Dokumente hochladen" and "Erwartete Rückläufe" and does not show "Lokale Prototyp-Nachverfolgung"
3. Return to Mitarbeitende and verify a canonical PNr-#### employee identifier remains visible

Expected: The Menu button safely exits the internal overlay into the main navigation, central Documents remains active, and no frontend insertion error occurs.