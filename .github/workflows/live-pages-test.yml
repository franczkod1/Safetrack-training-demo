name: SafeTrack deploy and public validation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: safetrack-pages
  cancel-in-progress: true

jobs:
  build:
    name: Validate and package SafeTrack
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate direct static source files
        run: |
          set -o pipefail
          node .github/scripts/validate-static-v9.mjs 2>&1 | tee STATIC_VALIDATION_RESULT.txt

      - name: Build exact GitHub Pages artifact
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site
          files=(
            index.html
            styles.css
            employee-training-groups.css
            seed-base.js
            trainings-a.js
            trainings-b.js
            data-final.js
            status-fixture.js
            app.js
            employee-training-groups.js
          )
          for file in "${files[@]}"; do
            test -s "${file}"
            cp "${file}" "_site/${file}"
          done
          touch _site/.nojekyll
          find _site -maxdepth 1 -type f -printf '%f\n' | sort

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Save static validation diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: safetrack-static-validation-${{ github.run_id }}
          path: STATIC_VALIDATION_RESULT.txt
          if-no-files-found: warn
          retention-days: 30

  deploy:
    name: Deploy SafeTrack to GitHub Pages
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    outputs:
      page_url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy exact Pages artifact
        id: deployment
        uses: actions/deploy-pages@v4

  public-test:
    name: Test deployed SafeTrack site
    runs-on: ubuntu-latest
    needs: deploy
    timeout-minutes: 15
    steps:
      - name: Checkout test scripts
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install browser test tools
        run: |
          npm init -y
          npm install playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Test the deployed GitHub Pages site
        id: smoke
        continue-on-error: true
        env:
          SAFETRACK_DEPLOYED_URL: ${{ needs.deploy.outputs.page_url }}
        run: node .github/scripts/live-test-v9.mjs

      - name: Save public test diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: safetrack-public-test-${{ github.run_id }}
          path: |
            LIVE_TEST_RESULT.json
            LIVE_TEST_SCREENSHOT.png
          if-no-files-found: warn
          retention-days: 30

      - name: Enforce public test result
        if: always()
        run: |
          test -s LIVE_TEST_RESULT.json
          node -e "const r=require('./LIVE_TEST_RESULT.json'); if(!r.passed){console.error(r); process.exit(1)}"
